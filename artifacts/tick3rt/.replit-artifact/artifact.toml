kind = "web"
previewPath = "/"
title = "Tick3t"
description = "Event ticketing platform with NFT tickets and template gallery"
version = "1.0.0"
id = "artifacts/tick3rt"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24122

[services.development]
run = "pnpm --filter @workspace/tick3t run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tick3t", "run", "build" ]
serve = "static"
publicDir = "artifacts/tick3rt/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24122"
BASE_PATH = "/"
